# Vue Mentorship

**Starter-шаблон для менторської програми** — стандартизоване робоче середовище з єдиними правилами та структурою для навчання перед переходом на індивідуальний To-Do проєкт.

## 🛠 Стек технологій

- **Vue 3** — прогресивний JavaScript фреймворк
- **TypeScript** — типізований JavaScript
- **Vite** — швидкий збірник і dev-сервер
- **Pinia** — сучасний стейт-менеджер для Vue
- **Tailwind CSS** — utility-first CSS фреймворк
- **ESLint** — лінтер коду
- **pnpm** — швидкий пакетний менеджер

## 📋 Передумови

Перед початком роботи переконайся, що у тебе встановлено:

- **Node.js LTS** (версія 18 або 20) — [завантажити](https://nodejs.org/)
- **pnpm** — встанови глобально: `npm install -g pnpm`
- **VS Code** з плагінами:
  - **Volar** (Vue Language Features)
  - **ESLint**

> **Примітка:** Prettier не використовується у цьому шаблоні — лише ESLint для перевірки якості коду.

## 🚀 Як запустити

1. **Клонуй або форкни** репозиторій
2. **Встанови залежності:**
   ```bash
   pnpm install
   ```
3. **Запусти сервер розробки:**
   ```bash
   pnpm dev
   ```

Застосунок буде доступний за адресою `http://localhost:5173`

### 🔧 Якщо щось не працює

- **Tailwind стилі не застосовуються?** → Перевір, що стилі імпортовано в `src/main.ts`
- **Volar не підсвічує Vue-файли?** → Перезавантажте VS Code або TypeScript Server (`Ctrl+Shift+P` → "TypeScript: Restart TS Server")
- **Помилки з Node.js?** → Використовуй Node LTS (18 або 20)

## 🔄 GitHub Flow (fork → feature → PR)

### Крок 1: Форк і клонування
```bash
# 1. Зроби fork цього репозиторію через GitHub UI
# 2. Клонуй свій fork
git clone https://github.com/YOUR_USERNAME/Mentorshipt.git
cd Mentorshipt
```

### Крок 2: Створи гілку для роботи
```bash
git checkout -b feature/<nickname>/<topic>
# Приклад: feature/ivan/add-header-component
```

### Крок 3: Розробка
```bash
# Зроби зміни у коді
# Перевір ESLint перед комітом
pnpm lint

# Додай зміни
git add .
git commit -m "Add header component with responsive design"
```

### Крок 4: Відправ PR
```bash
# Відправ гілку у свій fork
git push origin feature/<nickname>/<topic>

# Створи Pull Request із своєї гілки у гілку `dev` основного репозиторію
```

### Крок 5: Review процес
1. **Додай мітку** `needs review` до свого PR
2. **Дочекайся peer-review** від інших учасників
3. **Виправ зауваження** (якщо є)
4. **Дочекайся mentor review** для фінального схвалення

### 🌿 Призначення гілок

- **`main`** — стабільна версія, готова до продакшену
- **`dev`** — робоча гілка для інтеграції змін
- **`feature/<nickname>/<topic>`** — гілки учасників для нових функцій

## ✅ Peer-review чекліст

Перед схваленням PR перевір:

- [ ] **Код читається** — змінні та функції мають зрозумілі назви
- [ ] **Немає зайвих `console.log`** — видали debug-код
- [ ] **Логіка не дублюється** — винеси повторювані частини в окремі функції
- [ ] **ESLint проходить** — запусти `pnpm lint` без помилок
- [ ] **Розмір PR адекватний** — до 300–400 рядків змін
- [ ] **Опис PR зрозумілий** — що змінено і навіщо

## 🔒 Git Hooks і Husky

### Що таке Husky?

**Husky** - це інструмент, який дозволяє легко налаштовувати **Git hooks**. Git hooks - це скрипти, які автоматично виконуються при певних діях з Git (наприклад, перед коммітом).

### Як це працює в нашому проекті?

У цьому проекті налаштований **pre-commit hook**, який:

1. **Автоматично запускається** перед кожним `git commit`
2. **Перевіряє код** за допомогою ESLint
3. **Блокує коммміт**, якщо знайдені помилки в коді
4. **Дозволяє коммміт**, якщо код відповідає стандартам

### Що це означає для вас?

✅ **Переваги:**
- Неможливо закоммітити код з помилками форматування
- Автоматична перевірка якості коду
- Єдині стандарти коду в команді
- Менше помилок у pull request'ах

⚠️ **Що потрібно знати:**
- Якщо ваш код має помилки ESLint - коммміт буде заблоковано
- Спочатку виправте помилки за допомогою `pnpm lint`
- Тільки після успішної перевірки коммміт буде створено

### Приклад роботи:
```bash
# Спробуємо зробити коммміт
git commit -m "Мої зміни"

# Husky автоматично запустить перевірку:
🔍 Running ESLint check...

# Якщо є помилки:
❌ ESLint check failed. Please fix the errors before committing.
💡 You can run 'pnpm lint' to auto-fix some issues.

# Якщо все добре:
✅ ESLint check passed. Proceeding with commit...
```
### Як виправити помилки ESLint?

1. **Автоматичне виправлення:**
```bash
pnpm lint
```
2. **Тільки перевірка (без виправлень):**
```bash
pnpm lint:check
```
3. **У крайньому випадку (НЕ РЕКОМЕНДУЄТЬСЯ):**
```bash
git commit --no-verify -m "message"
```

## 🌐 HTTP клієнт (Axios) - Навчальна заготовка

Проект містить заготовку Axios клієнта з TODO коментарями для самостійного налаштування.

### 🎯 Завдання для учнів:

#### 📚 Що потрібно вивчити та реалізувати:

1. **Request Interceptors** - як автоматично модифікувати всі вихідні запити
2. **Response Interceptors** - як обробляти відповіді та помилки централізовано  
3. **JWT Authentication** - робота з Bearer токенами
4. **Environment Variables** - налаштування API URL через .env файли
5. **Error Handling** - правильна обробка HTTP статусів (401, 403, 500)

#### 🔍 Ключові концепції для дослідження:

**Axios Interceptors:**
- Що таке interceptors і навіщо вони потрібні?
- Як додати заголовки до всіх запитів автоматично?
- Як централізовано обробляти помилки?

**Аутентифікація:**
- Як зберігати JWT токени в браузері?
- Що робити при отриманні 401 помилки?
- Як автоматично додавати Authorization заголовок?

**Змінні оточення:**
- Як налаштувати різні API URL для розробки та продакшену?
- Синтаксис VITE_* змінних у Vite проектах

#### 📝 Практичні кроки:

1. **Досліджуйте документацію** Axios про interceptors
2. **Розкоментуйте та доповніть** код у `src/utils/axios.ts`
3. **Створіть .env файл** з налаштуваннями API
4. **Протестуйте** роботу з різними HTTP методами
5. **Додайте обробку помилок** для різних статусів

#### 💡 Корисні ресурси для вивчення:

- [Axios Documentation - Interceptors](https://axios-http.com/docs/interceptors)
- [JWT Authentication Best Practices](https://auth0.com/blog/a-look-at-the-latest-draft-for-jwt-bcp/)
- [Vite Environment Variables](https://vitejs.dev/guide/env-and-mode.html)
- [MDN - Using Fetch](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch)

#### 🎓 Навчальні цілі:

Після виконання завдання ви зможете:
- Налаштовувати HTTP клієнти для сучасних веб-додатків
- Реалізовувати аутентифікацію через JWT токени
- Централізовано обробляти HTTP помилки
- Працювати зі змінними оточення
- Розуміти принципи роботи interceptors

#### ⚠️ Важливі моменти для самостійного дослідження:

- Як правильно зберігати токени в браузері?
- Чи потрібно автоматично перенаправляти на логін при 401?
- Як обробляти network errors vs HTTP errors?
- Коли використовувати localStorage vs sessionStorage?

### 🧪 Тестування:

Для перевірки роботи можете використати:
- [JSONPlaceholder](https://jsonplaceholder.typicode.com/) для тестових API запитів
- DevTools → Network tab для перегляду заголовків
- DevTools → Application → Local Storage для перевірки токенів

## 🎯 ESLint конфігурація
### Форматування:
- **Відступи:** 2 пробіли
- **Кавички:** подвійні (`"`)
- **Крапки з комою:** обов'язкові
- **Пробіли в об'єктах:** рівномірні `{ key: value }`
### TypeScript:
- Попередження для типу `any`
- Заборона неіспользуємих змінних
- Строга типізація
### Vue специфічні правила:
- PascalCase для компонентів
- camelCase для пропсів
- Правильне форматування атрибутів

## 📏 Конвенції розробки

### Git робочий процес:
1. **Створіть нову гілку:** `feature/<nickname>/<topic>`
2. **Робіть невеликі коммміти** з описовими повідомленнями
3. **Перевіряйте код:** `pnpm lint:check` перед PR
4. **Створюйте PR** у гілку `dev`

### Стандарти коду:
- Максимальна довжина рядка: 100 символів
- Використовуйте TypeScript типи замість `any`
- Додавайте коментарі для складної логіки
- Слідуйте принципам Vue 3 Composition API

### Pull Request:
- **Розмір:** до 300-400 рядків змін
- **Опис:** ясний та детальний
- **Peer-review:** обов'язковий перед mentor review
- **Тестування:** переконайтеся, що все працює
## 🤝 Допомога
1. Перевірте, чи проходить код ESLint перевірку
2. Запустіть `pnpm lint` для автоматичного виправлення
3. Зверніться до ментора за допомогою
4. Створіть issue в репозиторії


## 📜 Скрипти

```bash
pnpm dev      # Запуск у режимі розробки (з hot reload)
pnpm build    # Білд застосунку для продакшену
pnpm preview  # Попередній перегляд збірки
pnpm lint     # Перевірка коду через ESLint
```

## 📁 Структура проєкту

```
src/
├── assets/           # Статичні файли (зображення, іконки)
├── components/       # Vue компоненти
│   └── common/       # Переважні компоненти
├── stores/           # Pinia стейт-менеджмент
├── views/            # Сторінки застосунку
│   └── Home.vue      # Головна сторінка
├── router/           # Vue Router конфігурація
├── types/            # TypeScript типи
├── utils/            # Допоміжні функції
├── main.ts           # Точка входу
└── main.scss         # Глобальні стилі
```

## 📏 Правила якості

1. **Маленькі, сфокусовані PR** — краще 5 невеликих PR ніж 1 величезний
2. **ESLint обов'язковий** — запускай `pnpm lint` перед кожним PR
3. **Зрозумілі назви** — гілки, коміти та змінні мають описувати суть
4. **Peer-review перед mentor review** — використовуй чекліст вище
5. **Чистий код** — жодних випадкових `console.log` у фінальному коді

## ❓ FAQ

**Q: Tailwind стилі не застосовуються до компонентів?**
A: Перевір, що файл стилів підключено в `src/main.ts`: `import './main.scss'`

**Q: Volar не підсвічує код у Vue файлах?**
A: Перезавантажте VS Code або TypeScript Server через Command Palette (`Ctrl+Shift+P`)

**Q: ESLint видає помилки про невідомі правила?**
A: Переконайся, що всі залежності встановлено: `pnpm install`

**Q: Як переключитися між гілками?**
A: Використовуй `git checkout <branch-name>` або `git switch <branch-name>`

---

## 🔗 Корисні посилання

- **Репозиторій:** [https://github.com/MortyQ/Mentorshipt](https://github.com/MortyQ/Mentorshipt)
- **Vue 3 документація:** [https://vuejs.org/](https://vuejs.org/)
- **Tailwind CSS:** [https://tailwindcss.com/](https://tailwindcss.com/)
- **Pinia:** [https://pinia.vuejs.org/](https://pinia.vuejs.org/)

---

**Удачі у навчанні! 🚀**
